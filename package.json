{
  "name": "nodeon",
  "version": "0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm install --prefix language-server && npm install --prefix vscode-extension",
    "build:lsp": "npm run build --prefix language-server",
    "build:ext": "npm run build --prefix vscode-extension",
    "build:all": "npm run build:lsp && npm run build:ext"
  },
  "repository": {
    "type": "git",
    "url": "git@github-home:isma-is-dev/Nodeon.git"
  },
  "author": "",
  "license": "ISC",
  "bin": {
    "nodeon": "./cli/bin.js"
  },
  "dependencies": {
    "ts-node": "10.9.2",
    "typescript": "5.3.3",
    "tsconfig-paths": "4.2.0"
  },
  "devDependencies": {
    "@types/node": "20.11.0"
  }
}
